apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  ports:
  - targetPort: 80
    port: 80
    nodePort: 30008

  selector:
    # For the selector information, take info from the pod def file.
    app: myapp
    type: front-end
    